<h1><%= @room.name %></h1>
<div class="card border-danger mb-3" style="max-width: 36rem;">
  <div class="card-header">相談内容</div>
  <div class="card-body text-danger">
    <h5 class="card-title"><%= @room.content %></h5>
  </div>
</div>

<div>
  <%= render partial: 'message', collection: @messages %>
</div>

<%= form_with model: [@room, @message], html: {class: "form"}, local: true do |f|%>
  <%= render 'shared/error_messages', model: f.object %>
  <div class="form-input">
    <%= f.text_area :content, class: 'form-control', placeholder: 'type a message' %>
  </div>
  <%= f.submit '送信', class: 'form-submit' %>
<% end %>

<% if current_user.mentor == 0 %>
  <button type="button" class="btn btn-outline-light btn-sm"><%= link_to "戻る", root_path %></button>
<% elsif current_user.mentor == 1 %>
  <button type="button" class="btn btn-outline-light btn-sm"><%= link_to "戻る", rooms_list_up_mentors_path %></button>
<% end %>